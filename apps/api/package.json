{
  "name": "@mapnova/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "lint": "eslint src",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply mapnova --local",
    "db:migrate:remote": "wrangler d1 migrations apply mapnova --remote"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.1",
    "@mapnova/shared": "workspace:*",
    "@sentry/cloudflare": "^10.51.0",
    "drizzle-orm": "^0.36.4",
    "hono": "^4.6.0",
    "jose": "^5.9.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260505.1",
    "drizzle-kit": "^0.28.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0",
    "wrangler": "^3.80.0"
  }
}
